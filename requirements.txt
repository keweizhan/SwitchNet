# Core ASR and evaluation
openai-whisper
torch
numpy
librosa
soundfile
jiwer

# Streamlit demo app
streamlit
pandas

# Plotting (scripts/plot_results.py, demo notebook)
matplotlib

# Optional: WhisperX backend (scripts/run_eval_whisperx.py)
# Requires a working PyTorch + CUDA environment; install separately:
#   pip install whisperx
